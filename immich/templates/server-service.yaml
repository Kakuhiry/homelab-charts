apiVersion: v1
kind: Service
metadata:
  name: immich-server
  namespace: {{ .Release.Namespace }}
  labels:
    app: immich-server
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 2283
      protocol: TCP
      name: http
  selector:
    app: immich-server

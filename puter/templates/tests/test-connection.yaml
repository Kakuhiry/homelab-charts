apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "puter.fullname" . }}-test-connection"
  labels:
    {{- include "puter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "puter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

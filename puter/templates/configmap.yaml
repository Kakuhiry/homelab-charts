apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
data: |-
  {{- toYaml .Values.configMap | toJson | b64enc | nindent 2 }}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sops-secrets-operator
  namespace: argocd
  annotations:
    # Must exist before anything that ships a *.sops.yaml SopsSecret (e.g.
    # torklink's discord-bot-token) so those resources have a controller to
    # reconcile them from the moment they land.
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: default
  source:
    repoURL: https://isindir.github.io/sops-secrets-operator
    chart: sops-secrets-operator
    targetRevision: 0.28.1
    helm:
      values: |
        secretsAsFiles:
          - name: sops-age-key-file
            mountPath: /etc/sops-age-key-file
            secretName: sops-age-key-file
        extraEnv:
          - name: SOPS_AGE_KEY_FILE
            value: /etc/sops-age-key-file/key
  destination:
    server: https://kubernetes.default.svc
    namespace: sops
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

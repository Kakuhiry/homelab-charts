# romm-secret is now managed via romm-secret.sops.yaml in this same
# templates/ dir (SOPS-encrypted). This used to template from
# .Values.secrets, which was intentionally left empty in values.yaml
# ("never commit real secrets here") -- meaning a full re-sync of this
# chart would have rendered an empty Secret and clobbered the real
# (out-of-band) one. Replaced with a proper SopsSecret instead.

apiVersion: v1
kind: Secret
metadata:
  name: romm-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}

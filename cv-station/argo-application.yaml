apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cv-station
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: git@github.com:Kakuhiry/homelab-charts.git
    targetRevision: HEAD
    path: cv-station
  destination:
    server: https://kubernetes.default.svc
    namespace: cv-station
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

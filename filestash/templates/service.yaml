{{ include "common.service" .}}
---
apiVersion: v1
kind: Service
metadata:
  name: onlyoffice
  labels:
    {{- include "filestash.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: onlyoffice

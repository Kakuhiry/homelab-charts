apiVersion: v2
name: minio
description: MinIO object storage for Loki log chunks
type: application
version: 0.1.0
dependencies:
  - name: minio
    version: "5.4.0"
    repository: https://charts.min.io/

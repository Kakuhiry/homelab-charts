apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-homelab
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  homelab.json: {{ .Files.Get "files/homelab-dashboard.json" | toJson }}

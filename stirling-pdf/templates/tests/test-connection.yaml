apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stirling-pdf.fullname" . }}-test-connection"
  labels:
    {{- include "stirling-pdf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stirling-pdf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
